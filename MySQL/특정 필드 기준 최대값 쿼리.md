
```sql
SELECT *
FROM (
	select *
    from main_sample
    where (processid) in(
	select max(processid) as processid
	from main_sample
	group by serialnumber
    ) 
) temp
GROUP BY temp.serialnumber;
```

